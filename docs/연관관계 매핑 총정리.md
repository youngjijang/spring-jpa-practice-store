단방향 연관관계

- 연관관계 주인이 아닌 경우
  - 연관관계 주인은 저장시점에 fk를 알 수 없기 때문에 데이터 저장시 insert + update 패턴으로 저장됨. (not null의 경우 저장실패)
  - one to one 관계일때는, 존재여부(null여부)를 알 수 없기때문에 프록시 객체를 알기위해서는 lazy load가 안됨. (one to many의 경우 빈 컬렉션이라도, 프록시 컬렉션을 만들 수 있기때문에 ㄱㅊ)
- 연관관계의 주인일 경우